--[[

	This is where we put settings(variables) that should be used all over the game

]]--

-- When two items collide, the "power" needs to reach this value for anything to actually happen
-- Why? Because if two things are just touching, rather than crashing, it shouldnt count in most cases
constGamePowerAbsorb = 20;
